diff --git a/client/simple.coffee b/client/simple.coffee
index 55a611a..b31be98 100644
--- a/client/simple.coffee
+++ b/client/simple.coffee
@@ -10,8 +10,6 @@ connection = new WebSocket('ws://127.0.0.1:8088')
 
 connection.setAddHistoryLine = (f) ->
     connection.addHistory = f
-connection.setChooseNick = (f) ->
-    connection.chooseNick = f
 
 connection.onopen = () ->
 
@@ -41,14 +39,9 @@ addLine = (line) ->
     #lines = [] if lines is undefined
     #localHistory.push line
     connection.addHistory line 
-setNick = (nick) ->
-    window.localStorage.setItem 'nick', nick
-    connection.chooseNick nick
 acceptNick = (nick) ->    
     console.log 'accept ', nick
     #sessionStorage.nickName = nick
-    setNick nick
+    window.localStorage.setItem 'nick', nick
 refuseNick = (nick) ->    
     console.log 'refuse ', nick
-    nonick = '('+nick+')'
-    setNick nonick
diff --git a/client/simple.js b/client/simple.js
index a0b8a16..c5759ca 100644
--- a/client/simple.js
+++ b/client/simple.js
@@ -5,7 +5,7 @@ mylines = localHistory
 addEventListener('message', (e) -> connection.send(e))
 */
 
-var acceptNick, addLine, connection, refuseNick, setHistory, setNick;
+var acceptNick, addLine, connection, refuseNick, setHistory;
 
 connection = new WebSocket('ws://127.0.0.1:8088');
 
@@ -13,10 +13,6 @@ connection.setAddHistoryLine = function(f) {
   return connection.addHistory = f;
 };
 
-connection.setChooseNick = function(f) {
-  return connection.chooseNick = f;
-};
-
 connection.onopen = function() {};
 
 connection.onerror = function(error) {};
@@ -62,19 +58,11 @@ addLine = function(line) {
   return connection.addHistory(line);
 };
 
-setNick = function(nick) {
-  window.localStorage.setItem('nick', nick);
-  return connection.chooseNick(nick);
-};
-
 acceptNick = function(nick) {
   console.log('accept ', nick);
-  return setNick(nick);
+  return window.localStorage.setItem('nick', nick);
 };
 
 refuseNick = function(nick) {
-  var nonick;
-  console.log('refuse ', nick);
-  nonick = '(' + nick + ')';
-  return setNick(nonick);
+  return console.log('refuse ', nick);
 };
diff --git a/controller/loginController.coffee b/controller/loginController.coffee
index 3d6674a..4a4f4ed 100644
--- a/controller/loginController.coffee
+++ b/controller/loginController.coffee
@@ -1,11 +1,9 @@
 
 uChat.controller 'loginController',
     ($scope, $location, $routeParams,chatModel) -> 
-        connection.setChooseNick (l) ->
-            $scope.$apply () -> $scope.nick = l
-        $scope.nick = chatModel.getNickName()
         $scope.chooseHandle = () ->
-            connection.requestHandle($scope.nick)
+            #connection.setAddHistoryLine (l) -> chatModel.addLine l
+            connection.requestHandle($scope.login.handle)
             #$location.path('/chatsAvailable/');
             $location.path('/chat/');
         $scope.createRoom = () ->
diff --git a/controller/loginController.js b/controller/loginController.js
index b5170b0..3566e27 100644
--- a/controller/loginController.js
+++ b/controller/loginController.js
@@ -1,14 +1,8 @@
 // Generated by CoffeeScript 1.6.1
 
 uChat.controller('loginController', function($scope, $location, $routeParams, chatModel) {
-  connection.setChooseNick(function(l) {
-    return $scope.$apply(function() {
-      return $scope.nick = l;
-    });
-  });
-  $scope.nick = chatModel.getNickName();
   $scope.chooseHandle = function() {
-    connection.requestHandle($scope.nick);
+    connection.requestHandle($scope.login.handle);
     return $location.path('/chat/');
   };
   $scope.createRoom = function() {
diff --git a/server/WSServer.coffee b/server/WSServer.coffee
index 0e64a59..34c62b2 100644
--- a/server/WSServer.coffee
+++ b/server/WSServer.coffee
@@ -17,9 +17,6 @@ history = [ ];
 
 # list of currently connected clients (users)
 clients = [ ];
-
-userNames = { };
-userNames['nono'] = true
  
 ###
  * Helper function for escaping input strings
@@ -74,11 +71,7 @@ wsServer.on 'request', (request) ->
         if userName is false
             # remember user name
             userName = htmlEntities(message.utf8Data);
-            if userNames[userName]
-                connection.sendUTF(JSON.stringify({ type:'refuseNickname', data: userName }));
-                console.log((new Date()) + ' User refused (existing): ' + userName);
-                return
-            userNames[userName] = true
+            # TODO: check for existing users:
             connection.sendUTF(JSON.stringify({ type:'acceptNickname', data: userName }));
             console.log((new Date()) + ' User is known as: ' + userName);
             return 
diff --git a/server/WSServer.js b/server/WSServer.js
index 72814e2..de83333 100644
--- a/server/WSServer.js
+++ b/server/WSServer.js
@@ -1,6 +1,6 @@
 // Generated by CoffeeScript 1.6.1
 "use strict";
-var clients, history, htmlEntities, http, server, userNames, webSocketServer, webSocketsServerPort, wsServer;
+var clients, history, htmlEntities, http, server, webSocketServer, webSocketsServerPort, wsServer;
 
 webSocketsServerPort = 8088;
 
@@ -17,10 +17,6 @@ history = [];
 
 clients = [];
 
-userNames = {};
-
-userNames['nono'] = true;
-
 /*
  * Helper function for escaping input strings
 */
@@ -70,15 +66,6 @@ wsServer.on('request', function(request) {
     }
     if (userName === false) {
       userName = htmlEntities(message.utf8Data);
-      if (userNames[userName]) {
-        connection.sendUTF(JSON.stringify({
-          type: 'refuseNickname',
-          data: userName
-        }));
-        console.log((new Date()) + ' User refused (existing): ' + userName);
-        return;
-      }
-      userNames[userName] = true;
       connection.sendUTF(JSON.stringify({
         type: 'acceptNickname',
         data: userName
diff --git a/uchat.html b/uchat.html
index 9f8ce0f..456cdfd 100644
--- a/uchat.html
+++ b/uchat.html
@@ -11,8 +11,9 @@
     </head>
 <body>
     <h1> Chatty things</h1>
-    <a href="#/login/">New handle here</a>
     <div ng-view>
     </div>
+    <p> below view</p>
+    <a href="#/login/">New handle here</a>
 </body>
 </html>
diff --git a/view/chat.html b/view/chat.html
index 2fe75df..4219e60 100644
--- a/view/chat.html
+++ b/view/chat.html
@@ -1,6 +1,5 @@
 <h2>the Chat</h2>
 <form name='newMessage'>
-    <p>You nick is: {{nick}}</p>
     <label for='loginContent'>New message</label>
     <input id='loginContent' ng-model='chat.message'></input>
     <!--input id='loginContent' ng-model='nick'></input-->
